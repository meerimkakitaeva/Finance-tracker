import React from 'react';
import Toolbar from "../../components/Toolbar/Toolbar";

const Home = () => {
    return (
        <div>
            <Toolbar />
        </div>
    );
};

export default Home;